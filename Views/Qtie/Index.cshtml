@model IEnumerable<stackoverflow.Models.QtieModel>

@{
    ViewData["Title"] = "Home Page";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QuestionModel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TagModel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionModel.QuestionID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TagModel.TagID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.QtieID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QtieID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QtieID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
